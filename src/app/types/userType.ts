export type User = {
  name: string;
  photo: string;
  theme: string;
  id: number;
  localStorageId: string;
  backgroundColor: string;
  textColor: string;
  fieldColor: string;
  navBarColor: string;
};
